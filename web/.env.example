# MSW を有効にして /v1/rounds/* をモックで返す (0 = 実APIを使用, 1 = MSWモック)
NEXT_PUBLIC_API_MOCK=1

# Phase 1 バックエンドAPI のベースURL (NEXT_PUBLIC_API_MOCK=0 の場合に使用)
# 本番: https://vgm-quiz-api.nantos.workers.dev
# ローカル開発: http://localhost:8787
NEXT_PUBLIC_API_BASE_URL=https://vgm-quiz-api.nantos.workers.dev

# /play を開いたら自動的にラウンド開始 (0 = 手動, 1 = 自動)
NEXT_PUBLIC_PLAY_AUTOSTART=1

# 追加のデバッグログを出す場合は 1
NEXT_PUBLIC_DEBUG=0
