# E2E: Matrix（#812）結果

## 結果（2025-09-12）
- **緑化**：matrix（ブラウザ×ビューポート×ロケール）一式がパス。

## 安定化の背景
- #809〜#811 で導入した **E2E専用の堅牢化**（MC固定／前進ループ／live region待機の寛容化／結果クローズ非致命）により、行列の差分（可視・トランジション・レスポンシブ）での揺れを吸収。

## 影響
- いずれも **テスト専用**の待機・フォールバックであり、**本番挙動は不変**。

## 備忘（運用）
- 回帰時はまず `features {{mode: ...}}`・`DATASET ready`・`[E2E] step=...` の3系列ログを確認。
- 落ちた環境でのみ `--trace on` を使い、可視条件（`display`/`visibility`/`aria-hidden`）の差を特定。

